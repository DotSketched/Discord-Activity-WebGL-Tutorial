{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tunnel": "cloudflared tunnel --url http://localhost:5173/"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^1.2.0"
  }
}
